# Importing these modules causes them to call pipeline_registry.register()
from infrastructure.embeddings.pipelines import resume_pipeline  
from infrastructure.embeddings.pipelines import job_pipeline     