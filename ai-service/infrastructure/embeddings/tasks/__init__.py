from infrastructure.embeddings.tasks.task_registry import run_task, get, TaskConfig
from infrastructure.embeddings.tasks.tasks import (
    run_skills,
    run_work_experience,
    run_certifications,
    run_job_title,
    run_location,
    run_requirements,
    run_experience_level,
)