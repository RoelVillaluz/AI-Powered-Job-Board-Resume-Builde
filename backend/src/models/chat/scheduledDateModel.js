import mongoose from "mongoose";

const scheduledEventSchema = new mongoose.Schema({
    // name
    // Event
    // timezone
    // users involved
})

const ScheduledEvent = mongoose.model('Scheduled Event', scheduledEventSchema)
export default ScheduledEvent