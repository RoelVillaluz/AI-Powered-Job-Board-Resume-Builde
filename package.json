{
  "name": "job-board-website-with-resume-maker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run ai\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "ai": "cd ai-service && python -m uvicorn app:app --reload",
    "test": "cd backend && npm test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^10.1.0"
  }
}